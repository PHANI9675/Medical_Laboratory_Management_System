package com.lab_processing.lps.entity.enums;

public enum ResultStatus {
    DRAFT,      // Entered but not verified
    VERIFIED,   // Verified by authorized user
    REPORTED,
    ENTERED,
    APPROVED// Included in final report
}